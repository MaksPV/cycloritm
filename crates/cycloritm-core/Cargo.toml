[package]
name = "cycloritm-core"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Validation, cycle expansion and get_schedule for Cycloritm"

[lib]
name = "cycloritm_core"
path = "src/lib.rs"

[dependencies]
cycloritm-parser = { path = "../cycloritm-parser" }
